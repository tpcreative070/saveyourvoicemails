<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.tpcreative.saveyourvoicemails">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".common.services.SaveYourVoiceMailsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Saveyourvoicemails">
        <activity android:name=".presentationlayer.player.PlayerAct">
        </activity>
        <activity
            android:name=".presentationlayer.user.view.SignUpAct"
            android:label="@string/title_activity_sign_up" />
        <activity android:name=".presentationlayer.user.view.SignInAct" />
        <activity android:name=".presentationlayer.home.view.HomeAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".common.services.SaveYourVoiceMailsService"
            android:foregroundServiceType="mediaProjection" />
    </application>

</manifest>